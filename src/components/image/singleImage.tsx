import styled, { StyledInterface } from "styled-components";

const Image: StyledInterface = styled.img`
    width: 100%;
    height: 300px;
`;

const SingleImage = () => {};

export default SingleImage;
